namespace EnDejtTillApplication.Models
{
    internal class Player
    {

        /*
         Name (string)
         PlayerCards (int) - lista med cards
         IsTurn (bool) - visar om det är spelarens tur att dra ett kort.
         MaxHandSize (int) - anger max antal kort spelaren kan ha på handen. 10 stycken. 
         HasCard (bool) - visar om spelaren har ett kort eller om det är det första kortet spelaren ska få. 
         */
    }
}
